# AI掘金头条新闻系统 —— 后端项目设计与分析文档

> 本文件是一份**复习/学习用**的完整设计与分析文档，内容与当前代码一一对应（对照 `main.py`、`config/`、`models/`、`schemas/`、`crud/`、`routers/`、`utils/`、`cache/` 实际源码编写）。
>
> 配合阅读：`README.md`（快速上手）、`API接口规范文档.md`（接口请求/响应示例）。

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术栈](#2-技术栈)
3. [目录结构与分层架构](#3-目录结构与分层架构)
4. [一次请求的完整链路](#4-一次请求的完整链路)
5. [配置管理](#5-配置管理)
6. [数据库设计](#6-数据库设计)
7. [ORM 模型层详解](#7-orm-模型层详解)
8. [Pydantic 校验层（schemas）](#8-pydantic-校验层schemas)
9. [数据访问层（crud）](#9-数据访问层crud)
10. [路由层（routers）](#10-路由层routers)
11. [认证机制](#11-认证机制)
12. [密码安全](#12-密码安全)
13. [统一响应格式](#13-统一响应格式)
14. [全局异常处理](#14-全局异常处理)
15. [Redis 缓存设计](#15-redis-缓存设计)
16. [API 接口清单](#16-api-接口清单)
17. [测试](#17-测试)
18. [关键设计决策与知识点](#18-关键设计决策与知识点)
19. [已知问题与改进建议](#19-已知问题与改进建议)
20. [新手复习路线](#20-新手复习路线)

---

## 1. 项目概述

一个基于 **FastAPI + SQLAlchemy（异步）** 构建的**仿今日头条新闻系统后端 API 服务**。

核心业务能力：

| 模块 | 能力 |
|---|---|
| 用户 | 注册、登录、获取/更新个人信息、修改密码 |
| 新闻 | 分类列表、新闻列表（分页 + 分类筛选）、详情（浏览量 +1、相关推荐） |
| 收藏 | 检查状态、添加、取消、列表、清空 |
| 浏览历史 | 添加（去重更新时间）、列表、删除单条、清空 |

整体特点：

- **全异步（async/await）**：Web 层、数据库访问、Redis 访问全部异步，单进程即可支撑较高并发。
- **经典分层架构**：`routers → crud → models → MySQL`，`schemas` 负责入参/出参校验，`cache/` 负责缓存。
- **统一响应 + 全局异常处理**：前端拿到的 JSON 结构统一为 `{code, message, data}`。
- **教学练习版**：源码中保留大量中文注释与"你来写"的模板引导，属正常现象。

---

## 2. 技术栈

| 技术 | 版本 | 用途 |
|---|---|---|
| Python | 3.11 | 编程语言 |
| FastAPI | 0.138 | Web 框架：路由、参数校验、自动生成 `/docs` |
| Uvicorn | 0.49 | ASGI 服务器，启动服务 |
| Pydantic | 2.13 | 请求/响应数据校验（`schemas/`） |
| pydantic-settings | 2.14 | 读取 `.env` / 环境变量配置 |
| SQLAlchemy | 2.0（异步） | ORM，用 Python 类操作数据库 |
| aiomysql | 0.3 | MySQL 异步驱动 |
| MySQL | — | 主数据库 |
| Redis（redis-py） | 8.0（异步） | 缓存热点数据（分类、新闻列表） |
| bcrypt | 5.0 | 密码加密与校验（原生 API，非 passlib） |
| pytest | — | 自动化测试 |

> 说明：`requirements.txt` 中已移除 `passlib`、`python-jose`、`rsa`、`ecdsa`、`pyasn1`、`six` 等未使用的 JWT 遗留依赖（git 中可看到该改动）。

---

## 3. 目录结构与分层架构

```
toutiao_backend/
├── main.py                    # 应用入口：创建 app、注册中间件/异常处理、挂载路由
├── requirements.txt           # 依赖清单
├── .env.example               # 环境变量示例（.env 被 gitignore）
├── config/                    # 配置层
│   ├── settings.py            #   pydantic-settings：DB / Redis 配置项与默认值
│   ├── db_conf.py             #   异步引擎、会话工厂、get_db 依赖
│   └── cache_config.py        #   从 settings 导出 Redis 连接参数
├── models/                    # ORM 模型层（Python 类 ↔ 数据库表）
│   ├── news.py                #   Base(带时间戳)、Category、News
│   ├── users.py               #   Base(空)、User、UserToken
│   ├── favorite.py            #   Base(空)、Favorite
│   └── history.py             #   Base(空)、History
├── schemas/                   # Pydantic 校验模型
│   ├── news.py                #   NewsItemBase
│   ├── users.py               #   UserRequest / UserInfoResponse / UserUpdateRequest / UserAuthResponse ...
│   ├── favorite.py            #   FavoriteCheckResponse / AddFavoriteRequest / FavoriteListResponse ...
│   └── history.py             #   AddHistoryRequest / HistoryListResponse ...
├── crud/                      # 数据访问层（数据库读写逻辑）
│   ├── news.py                #   分类 / 列表(带缓存) / 详情 / 浏览量+1 / 相关新闻
│   ├── users.py               #   注册 / 登录 / token / 更新资料 / 改密码
│   ├── favorite.py            #   收藏增删查清
│   └── history.py             #   历史增删查清
├── routers/                   # 路由层（URL、参数校验、调 crud、拼响应）
│   ├── news.py                #   /api/news/*
│   ├── users.py               #   /api/user/*
│   ├── favorite.py            #   /api/favorite/*（需认证）
│   └── history.py             #   /api/history/*（需认证）
├── utils/                     # 通用工具
│   ├── auth.py                #   认证依赖 get_current_user（解析 token）
│   ├── security.py            #   bcrypt 密码加密 / 校验
│   ├── response.py            #   统一成功响应 {code, message, data}
│   ├── exception.py           #   各类异常处理函数
│   ├── exception_handler.py   #   注册全局异常处理器
│   └── cache.py               #   Redis 底层读写封装（自动降级）
├── cache/
│   └── news_cache.py          # Redis 缓存键封装（news:categories、news:list:*）
├── tests/                     # pytest 测试（纯函数单元测试）
│   └── test_utils/
│       ├── test_security.py
│       └── test_response.py
├── API接口规范文档.md          # 接口详细规范（请求/响应示例）
└── 项目后端设计说明文档.md      # 本文件
```

### 分层职责

| 层 | 职责 | 关键点 |
|---|---|---|
| **routers** | 定义 URL、校验 Query/Body 参数、调用 crud、拼装响应 | 不写复杂 SQL，只做"胶水" |
| **crud** | 数据库读写逻辑 | 只处理数据，不关心 HTTP |
| **models** | ORM 映射，Python 类 ↔ 表 | `Mapped[...]` + `mapped_column(...)` |
| **schemas** | 请求体/响应体校验 | Pydantic，`alias` 做驼峰/下划线转换 |
| **utils** | 认证、加密、响应、异常、缓存等横切能力 | 各模块复用 |
| **cache** | 缓存键的组织与语义封装 | 底层调用 `utils/cache.py` |

---

## 4. 一次请求的完整链路

以 `GET /api/news/list?categoryId=1&page=1&pageSize=10` 为例：

```
客户端请求
    │
    ▼
① routers/news.py  get_news_list()
    - categoryId: Query(...) 必填
    - page: 默认 1；pageSize: 默认 10，le=100
    - db 通过 Depends(get_db) 注入
    ▼
② crud/news.py  get_news_list_cached()
    - 先查 Redis：news_cache.get_cache_news_list(1, 1, 10)  → 键 news:list:1:1:10
    - 命中 → 直接返回缓存的 dict
    - 未命中 → 继续
    ▼
③ crud/news.py  list_news() + news_count()
    - list_news: SELECT ... FROM news WHERE category_id=1 LIMIT 10 OFFSET 0
    - news_count: SELECT count(*) FROM news WHERE category_id=1
    ▼
④ 拼装结果 {list, total, hasMore} → jsonable_encoder 转可序列化 dict
    ▼
⑤ 回填缓存 news:list:1:1:10（TTL 7200s）
    ▼
⑥ routers 返回 {"code":200, "message":"success", "data":{...}}
```

**要点**：路由层负责参数校验与响应包装；crud 层负责"先缓存后库"的查询编排；缓存回填由 crud 内部完成，路由层无感知。

---

## 5. 配置管理

### 5.1 配置来源（`config/settings.py`）

使用 **pydantic-settings** 的 `BaseSettings`，读取优先级：

```
环境变量 > .env 文件 > 代码默认值
```

```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_file=".env", env_file_encoding="utf-8", extra="ignore"
    )
    DB_HOST: str = "localhost"
    DB_PORT: int = 3306
    DB_USER: str = "root"
    DB_PASSWORD: str = ""
    DB_NAME: str = "news_app"
    DB_CHARSET: str = "utf8mb4"
    REDIS_HOST: str = "localhost"
    REDIS_PORT: int = 6379
    REDIS_DB: int = 0
    REDIS_PASSWORD: str = ""
```

`extra="ignore"`：`.env` 里多出的未知变量不会报错，直接忽略。

### 5.2 数据库配置（`config/db_conf.py`）

```python
DATABASE_URL = (
    f"mysql+aiomysql://{quote_plus(settings.DB_USER)}:{quote_plus(settings.DB_PASSWORD)}"
    f"@{settings.DB_HOST}:{settings.DB_PORT}/{settings.DB_NAME}?charset={settings.DB_CHARSET}"
)

engine = create_async_engine(
    DATABASE_URL, echo=True,
    pool_size=10, max_overflow=20, pool_recycle=3600
)

AsyncSessionLocal = async_sessionmaker(engine, expire_on_commit=False, class_=AsyncSession)
```

关键知识点：

| 配置项 | 作用 | 说明 |
|---|---|---|
| `mysql+aiomysql://` | 异步驱动连接串前缀 | aiomysql 是 MySQL 的异步驱动 |
| `quote_plus` | URL 编码用户名/密码 | 防止密码含 `@ : / %` 导致 URL 解析错误 |
| `echo=True` | 打印每条 SQL | 学习阶段便于观察；上线关闭 |
| `pool_size` | 连接池常驻连接数 | 默认 10 |
| `max_overflow` | 超出常驻后可临时多开的连接数 | 峰值 10+20=30 |
| `pool_recycle` | 连接回收周期（秒） | MySQL 默认 wait_timeout 8 小时，设短点防失效 |
| `expire_on_commit=False` | commit 后对象不过期 | 否则异步里再访问属性会触发隐式 IO 报错 |

`get_db` 是**生成器依赖**：

```python
async def get_db():
    async with AsyncSessionLocal() as session:
        yield session
```

请求进入时开一个 session，请求结束（离开 `with`）自动关闭并归还连接。

### 5.3 Redis 配置（`config/cache_config.py`）

仅做一层"从 settings 导出"的转发，供 `utils/cache.py` 使用。

---

## 6. 数据库设计

共 **6 张表**（模型在 `models/`，当前无建表脚本 / Alembic migration，需手动建表）。

| 表 | 用途 | 关键字段与约束 |
|---|---|---|
| `user` | 用户 | `username` 唯一、`password`(bcrypt 密文)、`nickname`、`avatar`、`gender`(Enum)、`bio`、`phone`(唯一) |
| `user_token` | 登录令牌 | `user_id`(外键→user.id)、`token`(唯一)、`expires_at`(7 天过期) |
| `news_category` | 新闻分类 | `name`(唯一)、`sort_order` |
| `news` | 新闻 | `title`、`description`、`content`、`image`、`author`、`category_id`(外键)、`views`、`publish_time`；索引 `category_id`、`publish_time` |
| `favorite` | 收藏 | `user_id` + `news_id`（联合唯一约束，防重复收藏） |
| `history` | 浏览历史 | `user_id`、`news_id`、`view_time` |

### 字段细节

**user 表**（`models/users.py`）：

- `username`：`String(50)` 唯一非空
- `password`：`String(255)` 非空，存 bcrypt 密文（bcrypt 输出约 60 字符，255 留足余量）
- `avatar`：默认值 `https://fastly.jsdelivr.net/npm/@vant/assets/cat.jpeg`
- `gender`：`Enum('male','female','unknown')`，默认 `unknown`
- `bio`：默认 `'这个人很懒，什么都没留下'`
- `phone`：`String(20)` 唯一（可空）
- 索引：`username_UNIQUE`、`phone_UNIQUE`

**user_token 表**：

- `token`：`String(255)` 唯一非空，值为 `uuid4` 字符串
- `expires_at`：`DateTime` 非空，7 天过期
- 索引：`token_UNIQUE`、`fk_user_token_user_idx`

**news_category 表**（`models/news.py`）：

- `name`：`String(50)` 唯一非空
- `sort_order`：int 非空，默认 0
- 继承自带时间戳的 `Base` → 自动含 `created_at` / `updated_at`

**news 表**：

- `title`：`String(255)` 非空
- `description`：`String(500)` 可空
- `content`：`Text` 非空
- `image` / `author`：可空
- `category_id`：外键 → `news_category.id`，非空
- `views`：int 非空默认 0
- `publish_time`：默认 `CURRENT_TIMESTAMP`
- 索引：`idx_news_category_id`、`idx_news_publish_time`

**favorite 表**（`models/favorite.py`）：

- `id` 主键自增
- `user_id`、`news_id` 非空（注意：模型里**未显式写 ForeignKey**，但表结构上有外键）
- `created_at`：`default=datetime.now`
- `UniqueConstraint("user_id","news_id")` 防止重复收藏
- 索引：`idx_favorite_user_id`、`idx_favorite_news_id`

**history 表**（`models/history.py`）：

- `view_time`：注意模型定义为 `Mapped[str]`（字符串），但业务写入的是 `datetime.now()`，响应 schema 里声明为 `datetime`——存在类型不一致（见"已知问题"）
- 索引：`fk_history_user_idx`、`fk_history_news_idx`、`idx_view_time`

---

## 7. ORM 模型层详解

### 7.1 SQLAlchemy 2.x 新写法

- 用 `Mapped[类型]` 做类型注解，`mapped_column(...)` 定义列。
- SQL 列类型由 Python 类型自动推断：`int→Integer`、`str→String`、`datetime→DateTime`。
- 需要指定长度时才显式写类型，如 `mapped_column(String(32))`。
- 自定义基类用 `class Base(DeclarativeBase)`，不再用 `Base = declarative_base()`。

### 7.2 带时间戳的 Base（`models/news.py`）

```python
class Base(DeclarativeBase):
    created_at: Mapped[datetime] = mapped_column(server_default=func.now(), comment="创建时间")
    updated_at: Mapped[datetime] = mapped_column(server_default=func.now(), onupdate=func.now(), comment="更新时间")
```

- `created_at`：插入时由**数据库**填当前时间（`server_default=func.now()`）。
- `updated_at`：插入时填当前时间 + 每次更新自动刷新（`onupdate=func.now()`）。
- 继承此 Base 的子表自动带上这俩字段。

### 7.3 多个 Base 的问题（重要）

`models/` 下存在**多个独立的 `DeclarativeBase`**：

| 文件 | Base 定义 | 是否带时间戳 |
|---|---|---|
| `news.py` | `class Base(DeclarativeBase)` | 是（created_at / updated_at） |
| `users.py` | `class Base(DeclarativeBase)` | 否（空，User/UserToken 自己手写时间字段） |
| `favorite.py` | `class Base(DeclarativeBase)` | 否（空，Favorite 自己写 created_at） |
| `history.py` | `class Base(DeclarativeBase)` | 否（空，History 自己写 view_time） |

影响：这些 `Base` **不是同一个类**，它们的 metadata 相互独立。虽然本项目当前没有用 `Base.metadata.create_all` 建表（手动建表），但如果将来做迁移或建表脚本，必须统一 Base，否则不同文件定义的表不会被注册到同一个 metadata。这是复习时应重点理解的一个"坑"。

---

## 8. Pydantic 校验层（schemas）

### 8.1 核心配置项

| 配置 | 作用 |
|---|---|
| `from_attributes=True` | 允许 `Model.model_validate(orm_obj)` 从 ORM 对象属性取值 |
| `populate_by_name=True` | 同时接受字段名与 alias（如 `category_id` 和 `categoryId` 都能填） |
| `Field(alias="...")` | 字段对外名用驼峰，对内名用下划线 |

### 8.2 关键模型

**`NewsItemBase`**（`schemas/news.py`）——新闻列表条目的公共基类：

```python
class NewsItemBase(BaseModel):
    id: int
    title: str
    description: Optional[str] = None
    image: Optional[str] = None
    author: Optional[str] = None
    category_id: int = Field(alias="categoryId")
    views: int
    publish_time: Optional[datetime] = Field(None, alias="publishTime")
```

**`UserAuthResponse`**——注册/登录响应（token + userInfo）：

```python
class UserAuthResponse(BaseModel):
    token: str
    user_info: UserInfoResponse = Field(..., alias="userInfo")
```

**`FavoriteListResponse`** / **`HistoryListResponse`**——分页列表：

```python
class FavoriteListResponse(BaseModel):
    items: list[FavoriteItemBase] = Field(default=[], alias="list")
    total: int
    has_more: bool = Field(alias="hasMore")
```

> 注意：响应里 `list` 是 Python 关键字，所以字段名用 `items` + `alias="list"` 输出成 `"list"`。

**`FavoriteItemBase`** 继承 `NewsItemBase`，额外增加 `favoriteId`、`favoriteTime`；**`HistoryItemBase`** 额外增加 `viewTime`。这体现了"复用 + 扩展"的设计。

### 8.3 alias 驼峰/下划线转换总结

| 对外（前端/JSON） | 对内（Python 字段） |
|---|---|
| `categoryId` | `category_id` |
| `publishTime` | `publish_time` |
| `favoriteId` | `favorite_id` |
| `favoriteTime` | `favorite_time` |
| `viewTime` | `view_time` |
| `hasMore` | `has_more` |
| `userInfo` | `user_info` |
| `isFavorite` | `is_favorite` |
| `newsId` | `news_id` |
| `oldPassword` / `newPassword` | `old_password` / `new_password` |

---

## 9. 数据访问层（crud）

### 9.1 新闻（`crud/news.py`）

| 函数 | 职责 | 关键点 |
|---|---|---|
| `get_categories` | 分类列表（带缓存） | 先查缓存，未命中查库并回填；用 `jsonable_encoder` 把 ORM 对象转可序列化 dict |
| `list_news` | 查询新闻分页列表 | `select(News).where(...).offset(...).limit(...)` |
| `news_count` | 分类下新闻总数 | `select(func.count(News.id))` → `scalar_one()` |
| `get_news_list_cached` | 新闻列表（带缓存） | 编排：缓存→库→回填→返回 `{list,total,hasMore}` |
| `news_detail` | 单条新闻 | `scalar_one_or_none()`（不存在返回 None，不抛异常） |
| `increase_views` | 浏览量 +1 | `update(...).values(views=func.coalesce(News.views,0)+1)`，返回 `rowcount>0` |
| `get_related_news` | 相关新闻 | 同分类、排除自身、按浏览量倒序、limit |

分页 `hasMore` 计算：

```python
has_more = (page - 1) * page_size + page_size < total
# 即：offset + page_size < total
```

### 9.2 用户（`crud/users.py`）

| 函数 | 职责 | 关键点 |
|---|---|---|
| `get_user_data` | 按用户名查用户 | `scalar_one_or_none()` |
| `create_user` | 创建用户 | bcrypt 加密密码 → add → commit → refresh |
| `create_token` | 生成/刷新 token | `uuid4`，7 天过期；**存在则更新，不存在则新建**（一人一 token 记录） |
| `authenticate_user` | 登录校验 | 先查用户，再 `verify_password` |
| `get_user_by_token` | 按 token 查用户 | 先查未过期 token，再查 user |
| `update_user` | 更新资料 | `model_dump(exclude_unset=True, exclude_none=True)` 只更新传了的字段 |
| `change_password` | 改密码 | 先校验旧密码，再哈希新密码 |

`update_user` 的 `exclude_unset=True, exclude_none=True`：只保留前端**显式传入且非 None** 的字段，避免把未传字段误覆盖成 None。

### 9.3 收藏（`crud/favorite.py`）

| 函数 | 职责 | 关键点 |
|---|---|---|
| `check_favorite` | 是否已收藏 | 返回 bool |
| `add_favorite_crud` | 添加收藏 | 已收藏返回 None；否则 add→commit→refresh |
| `remove_favorite_crud` | 取消收藏 | `delete(...).where(...)`，返回 `rowcount>0` |
| `get_favorite_list_crud` | 收藏列表 | `select(Favorite, News).join(...)` 连表，拼 `FavoriteItemBase` |
| `clear_favorite_crud` | 清空收藏 | 返回删除条数 |

连表查询返回**元组** `(Favorite, News)`：

```python
stmt = (
    select(Favorite, News)
    .join(News, Favorite.news_id == News.id)
    .where(Favorite.user_id == user_id)
    .order_by(desc(Favorite.created_at))
    .offset(...).limit(...)
)
rows = result.all()  # 每行 = (fav, news)
```

### 9.4 历史（`crud/history.py`）

| 函数 | 职责 | 关键点 |
|---|---|---|
| `add_history` | 添加浏览记录 | **去重**：已有则更新 `view_time`，无则新建 |
| `get_history_list` | 历史列表 | 同收藏：连表 + 分页 + 拼 `HistoryItemBase` |
| `delete_history` | 删除单条 | 加 `user_id` 条件，防止删别人的记录 |
| `clear_history` | 清空历史 | 返回删除条数 |

---

## 10. 路由层（routers）

### 10.1 路由前缀

```python
news     → APIRouter(prefix="/api/news",     tags=["news"])
users    → APIRouter(prefix="/api/user",     tags=["users"])
favorite → APIRouter(prefix="/api/favorite", tags=["收藏"])
history  → APIRouter(prefix="/api/history",  tags=["浏览历史"])
```

`main.py` 里 `app.include_router(...)` 挂载，路由文件内只写相对路径。

### 10.2 参数校验写法

```python
@router.get("/list")
async def get_news_list(
    categoryId: int = Query(..., description="分类ID"),   # 必填
    page: int = Query(1, description="页码，默认 1"),
    pageSize: int = Query(10, description="每页条数", le=100),  # ≤100
    db: AsyncSession = Depends(get_db),
):
    ...
```

- `Query(...)`：`...`（Ellipsis）表示必填。
- `le=100`：上限校验。
- 驼峰参数名（`categoryId`、`pageSize`）直接作为 Query 参数名，传给 crud 时用关键字参数转下划线。

### 10.3 各模块接口

**用户**（`routers/users.py`）：

| 方法 | 路径 | 认证 | 逻辑 |
|---|---|---|---|
| POST | `/register` | 否 | 查重→建用户→建 token→返回 token+userInfo |
| POST | `/login` | 否 | 认证→建 token→返回 |
| GET | `/info` | 是 | 直接返回 `current_user` |
| PUT | `/update` | 是 | 更新→404 或返回新信息 |
| PUT | `/password` | 是 | 改密码→失败 500 |

**新闻**（`routers/news.py`）：

| 方法 | 路径 | 逻辑 |
|---|---|---|
| GET | `/categories` | `skip`/`limit` |
| GET | `/list` | 分页 + 分类 |
| GET | `/detail` | 详情→404→浏览量+1→refresh→相关新闻 |

**收藏**（`routers/favorite.py`）、**历史**（`routers/history.py`）：均需 `Depends(get_current_user)`，详见 [API 接口清单](#16-api-接口清单)。

---

## 11. 认证机制

基于 **Token** 的认证（存库型，非 JWT）：

1. 注册/登录成功后生成随机 `uuid4` token，存入 `user_token` 表，有效期 **7 天**。
2. 前端后续请求在请求头携带 `Authorization: <token>`（兼容 `Authorization: Bearer <token>`）。
3. 需要认证的接口通过 `Depends(get_current_user)` 校验。

`utils/auth.py`：

```python
security_scheme = APIKeyHeader(name="Authorization")

async def get_current_user(auth_header=Depends(security_scheme), db=Depends(get_db)):
    if auth_header.startswith("Bearer "):
        token = auth_header[7:]
    else:
        token = auth_header
    user = await get_user_by_token(db, token)
    if not user:
        raise HTTPException(status_code=401, detail="无效的 token")
    return user
```

要点：

- 用 `APIKeyHeader` 而不是 `HTTPBearer`，因此**不强制** "Bearer " 前缀，两种格式都兼容。
- `get_current_user` 是一个**依赖**，依赖里又依赖 `get_db`（依赖嵌套注入）。
- token 校验在 SQL 层就过滤过期：`UserToken.expires_at > datetime.now()`。

---

## 12. 密码安全

`utils/security.py` 使用 **bcrypt 原生 API**（bcrypt 5.x，替代 passlib）：

```python
def get_hashed_password(password: str) -> str:
    return bcrypt.hashpw(password.encode("utf-8"), bcrypt.gensalt()).decode("utf-8")

def verify_password(plain_password: str, hashed_password: str) -> bool:
    return bcrypt.checkpw(plain_password.encode("utf-8"), hashed_password.encode("utf-8"))
```

关键点：

- bcrypt 操作 `bytes`，这里封装成 `str` 接口。
- `gensalt()` 每次随机 → **同一密码两次哈希结果不同**（测试覆盖了这点）。
- 哈希以 `$2b$` 开头。
- 校验用 `checkpw`，不自己比字符串。

---

## 13. 统一响应格式

`utils/response.py`：

```python
def success_response(message="success", data=None):
    content = {"code": 200, "message": message, "data": data}
    return JSONResponse(content=jsonable_encoder(content))
```

- 成功响应统一：`{ "code": 200, "message": "success", "data": {} }`。
- `jsonable_encoder` 把 datetime、ORM 对象、Pydantic 模型等转成可 JSON 序列化的类型。
- 返回的是 `JSONResponse` 对象（可直接作为路由返回值）。

> 注意：`routers/news.py` 的 `categories`、`list`、`detail` 三个接口是**直接 return 字典**，没用 `success_response`，但结构一致（`code/message/data`）。收藏、历史、用户模块用了 `success_response`。

---

## 14. 全局异常处理

`utils/exception_handler.py` 注册顺序："具体在前、兜底在后"：

```python
app.add_exception_handler(HTTPException, http_exception_handler)      # 业务
app.add_exception_handler(IntegrityError, integrity_error_handler)    # 数据完整性
app.add_exception_handler(SQLAlchemyError, sqlalchemy_error_handler)  # 数据库
app.add_exception_handler(Exception, general_exception_handler)       # 兜底
```

| 异常 | 场景 | HTTP 状态码 | 响应 message |
|---|---|---|---|
| `HTTPException` | 业务错误（用户名已存在、认证失败、新闻不存在） | 400/401/404 | 用 `exc.detail` |
| `IntegrityError` | 数据库约束冲突（用户名重复、外键不存在） | 400 | 根据错误信息细分文案 |
| `SQLAlchemyError` | 数据库操作失败 | 500 | "数据库操作失败" |
| `Exception` | 兜底 | 500 | "服务器内部错误" |

`IntegrityError` 通过 `str(exc.orig)` 判断具体约束：

- 含 `username_UNIQUE` 或 `Duplicate entry` → "用户名已存在"
- 含 `FOREIGN KEY` → "关联数据不存在"

**DEBUG_MODE**（`utils/exception.py` 顶部 `DEBUG_MODE = True`）：开发模式下，数据库/未知异常会在 `data` 里附带 `error_type`、`error_detail`、`traceback`、`path`，便于排查；上线前应置 False。

---

## 15. Redis 缓存设计

### 15.1 底层封装（`utils/cache.py`）

```python
redis_client = Redis(host=..., port=..., db=..., password=..., decode_responses=True)

async def get_cache(key)      # 原始字符串，异常返回 None
async def get_json_cache(key) # 读 + json.loads
async def set_cache(key, value, ex=3600)  # dict/list 自动 json.dumps，setex 带 TTL
```

- `decode_responses=True`：取出的值自动 decode 成 str。
- 每个读写函数都有 `try/except`，**Redis 不可用时自动降级**（返回 None / False），不影响主业务查库。

### 15.2 业务缓存键（`cache/news_cache.py`）

| 缓存键 | 内容 | TTL |
|---|---|---|
| `news:categories` | 新闻分类列表 | 7200s（2 小时） |
| `news:list:{category_id}:{page}:{size}` | 新闻列表分页结果 | 7200s（2 小时） |

### 15.3 缓存策略

采用 **Cache-Aside（旁路缓存）**：先查缓存 → 命中直接返回 → 未命中查库 → 回填缓存。

```python
async def get_news_list_cached(db, category_id, page, page_size):
    cached = await news_cache.get_cache_news_list(category_id, page, page_size)
    if cached:
        return cached
    news_list = await list_news(db, category_id, page, page_size)
    total = await news_count(db, category_id)
    result = {"list": jsonable_encoder(news_list), "total": total, "hasMore": ...}
    await news_cache.set_cache_news_list(category_id, page, page_size, result)
    return result
```

> 注意：README/旧设计文档提到的"新闻详情缓存 / 历史记录缓存 / 缓存主动失效"**尚未实现**，以代码实际为准（当前只有分类 + 列表缓存，且无写路径的缓存失效逻辑）。

---

## 16. API 接口清单

基础 URL：`http://localhost:8000`。完整请求/响应示例见 `API接口规范文档.md`。

### 用户 `/api/user`

| 方法 | 路径 | 认证 | 说明 |
|---|---|---|---|
| POST | `/register` | 否 | 注册，返回 `{token, userInfo}` |
| POST | `/login` | 否 | 登录，返回 `{token, userInfo}` |
| GET | `/info` | 是 | 获取当前用户信息 |
| PUT | `/update` | 是 | 更新昵称/头像/性别/简介/手机号 |
| PUT | `/password` | 是 | 修改密码（`oldPassword`/`newPassword`） |

### 新闻 `/api/news`（无需认证）

| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| GET | `/categories` | `skip`、`limit` | 分类列表 |
| GET | `/list` | `categoryId`(必填)、`page`、`pageSize`(≤100) | 新闻列表 |
| GET | `/detail` | `id`(必填) | 详情，浏览量+1，返回 `relatedNews` |

### 收藏 `/api/favorite`（需认证）

| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| GET | `/check` | `newsId` | 是否已收藏 → `{isFavorite}` |
| POST | `/add` | body `{newsId}` | 添加收藏 |
| DELETE | `/remove` | `newsId` | 取消收藏 |
| GET | `/list` | `page`、`pageSize` | 收藏列表 |
| DELETE | `/clear` | — | 清空收藏 |

### 浏览历史 `/api/history`（需认证）

| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| POST | `/add` | body `{newsId}` | 添加浏览记录（去重） |
| GET | `/list` | `page`、`pageSize` | 历史列表 |
| DELETE | `/delete/{history_id}` | 路径参数 | 删除单条 |
| DELETE | `/clear` | — | 清空历史 |

---

## 17. 测试

```powershell
.venv\Scripts\python.exe -m pytest tests -q
# 结果：14 passed
```

当前覆盖（**纯函数单元测试**，不依赖数据库/网络）：

- `tests/test_utils/test_security.py`：bcrypt 加密与校验（返回字符串、与原文不同、同密码两次哈希不同、`$2b$` 前缀、正确/错误/空密码校验）。
- `tests/test_utils/test_response.py`：统一响应（状态码 200、合法 JSON、`data` 传递、默认/自定义 message、`code` 恒为 200、`data=None`）。

测试结构采用 **AAA 模式**（Arrange / Act / Assert）注释，是新手学习 pytest 的良好范本。

> 接口级（集成）测试尚未补充。

---

## 18. 关键设计决策与知识点

1. **全异步栈**：FastAPI(异步路由) + `AsyncSession` + `redis.asyncio`，IO 全程不阻塞。
2. **分层解耦**：routers（HTTP）→ crud（数据）→ models（ORM），各层职责单一。
3. **依赖注入**：`Depends(get_db)`、`Depends(get_current_user)`，FastAPI 自动解析参数并管理 session 生命周期。
4. **Pydantic alias 机制**：用 `Field(alias=...)` + `populate_by_name=True` 实现前后端驼峰/下划线命名转换。
5. **token 存库而非 JWT**：`user_token` 表支持服务端主动失效、一人一条记录（登录会刷新同一条）。
6. **`expire_on_commit=False`**：避免异步场景下 commit 后访问对象属性触发隐式 IO。
7. **`jsonable_encoder`**：把 ORM 对象 / datetime 统一转成可序列化 dict 后再进缓存或响应。
8. **bcrypt 原生 API**：绕开 passlib 与 bcrypt 5.x 的兼容问题。
9. **缓存降级**：Redis 异常不抛错，自动回退查库，保证可用性。
10. **异常处理器顺序**：子类/具体异常在前，父类/兜底在后。

---

## 19. 已知问题与改进建议

> 复习时重点关注，很多是"能跑但有隐患"的细节。

| # | 问题 | 位置 | 建议 |
|---|---|---|---|
| 1 | **多个独立 Base 类** | `models/` | 统一为一个 Base（建议以 `news.py` 的带时间戳 Base 为准），否则将来 `metadata.create_all` / Alembic 只认其中一个 |
| 2 | **无建表脚本 / 无 Alembic** | 全项目 | 引入 Alembic，或补充 SQL 建表脚本 |
| 3 | **`history.view_time` 类型不一致** | `models/history.py` vs `schemas/history.py` | 模型 `Mapped[str]` 但写的是 `datetime.now()`，schema 声明 `datetime`；应统一为 `DateTime` |
| 4 | **删除历史失败文案错误** | `routers/history.py` | `remove_history` 失败时返回 `"未收藏"`，应为"记录不存在"等 |
| 5 | **删除/取消不存在的记录仍返回 200** | `routers/favorite.py`、`routers/history.py` | 业务上"未收藏/记录不存在"返回 code 200 而非 404，与异常处理设计不一致 |
| 6 | **改密码旧密码错误返回 500** | `routers/users.py` | `change_password` 返回 False 时抛 500，应区分"旧密码错误"（400/401）与真失败（500） |
| 7 | **CORS 全放通** | `main.py` | `allow_origins=["*"]` + `allow_credentials=True` 生产不可用（浏览器不允许通配符+凭证），应收紧 |
| 8 | **新闻列表未排序** | `crud/news.py` `list_news` | 模板注释有 `order_by(desc(publish_time))`，实际代码漏写，列表顺序不稳定 |
| 9 | **分类缓存键不含 skip/limit** | `crud/news.py` `get_categories` | 缓存命中时会忽略 skip/limit 参数，分页语义失效 |
| 10 | **缓存无失效机制** | 收藏/浏览历史写入 | 新闻列表/分类缓存固定 2 小时 TTL，数据变更时不会主动失效 |
| 11 | **token 未校验用户存在性边界** | `crud/users.py` `get_user_by_token` | token 有效但用户被删时返回 None，正常；但 token 记录不清理会积累 |
| 12 | **部分接口响应格式不统一** | `routers/news.py` | news 三接口直接 return dict（虽结构一致），建议统一走 `success_response` |

---

## 20. 新手复习路线

1. **跑起来**：`.venv\Scripts\python.exe main.py` → 打开 `http://127.0.0.1:8000/docs` 把所有接口点一遍，建立整体认知。
2. **入口**：读 `main.py`（创建 app、CORS、注册异常、挂载路由）。
3. **最简路由链路**：`routers/news.py` → `crud/news.py` → `models/news.py` → `schemas/news.py`，对照 [第 4 节](#4-一次请求的完整链路) 走通 `GET /api/news/list`。
4. **认证链路**：`utils/auth.py` → `crud/users.py:get_user_by_token` → `routers/users.py` 的需认证接口。
5. **缓存链路**：`utils/cache.py` → `cache/news_cache.py` → `crud/news.py:get_news_list_cached`。
6. **异常链路**：`utils/exception.py` + `utils/exception_handler.py`，理解处理器注册顺序与 DEBUG_MODE。
7. **测试**：`pytest tests -q`，再模仿现有测试为某个 crud 函数补一个单元测试。
8. **查缺补漏**：对照 [第 19 节](#19-已知问题与改进建议) 逐条验证并尝试修复，是加深理解的最佳练习。
